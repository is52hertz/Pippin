import Foundation
import MCP

// stdio ⇄ HTTP bridge. Implemented in step 7; this placeholder exists so the
// bundle layout and the package graph are complete from step 1 onward.
FileHandle.standardError.write(Data("pippin-shim: not implemented yet (step 7)\n".utf8))
exit(EXIT_FAILURE)
